name: push-with-path-filter
on:
  push:
    branches-ignore:
      - release
    paths:
      - "**.py"

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - name: echo text
        run: echo "Job ran successfully"
